apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-aks-deploy.fullname" . }}-test-connection"
  labels:
    {{- include "helm-aks-deploy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-aks-deploy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
